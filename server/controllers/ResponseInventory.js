class resposnse {
    constructor(status, sku, type, pricing, metadata) {
        this.status = status;
        this.sku = sku;
        this.type = type;
        this.pricing = pricing;
        this.metadata = metadata;
    }
}   
export default resposnse;